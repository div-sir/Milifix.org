---
import SpaceIndexPage from '../../components/pages/SpaceIndexPage.astro';
import { SPACE_IDS } from '../../data/spaces';

export function getStaticPaths() {
  return SPACE_IDS.map((space) => ({ params: { space } }));
}
---

<SpaceIndexPage lang="en" spaceParam={Astro.params.space} />

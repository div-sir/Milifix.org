---
import '../../styles/theme.css';
import '../../styles/portfolio-atmosphere.css';
import SiteNav from '../SiteNav.astro';
import AmbientBackdrop from '../AmbientBackdrop.astro';
import PortfolioChrome from '../PortfolioChrome.astro';
import type { Lang } from '../../i18n/types';
import { HTML_LANG } from '../../i18n/types';

interface Props {
  lang: Lang;
  pathname: string;
}

const { lang, pathname } = Astro.props;

// ── feature icons (inline SVG, no emoji) ──────────────────────────
const ICONS = {
  map: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><polygon points="3 6 9 3 15 6 21 3 21 18 15 21 9 18 3 21"/><line x1="9" y1="3" x2="9" y2="18"/><line x1="15" y1="6" x2="15" y2="21"/></svg>`,
  nfc: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M6 8.32a7.43 7.43 0 0 1 0 7.36"/><path d="M9.46 6.21a11.76 11.76 0 0 1 0 11.58"/><path d="M12.91 4.1a15.91 15.91 0 0 1 .01 15.8"/><path d="M16.37 2a20.16 20.16 0 0 1 0 20"/></svg>`,
  flight: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><path d="M17.8 19.2 16 11l3.5-3.5C21 6 21 4 21 4s-2 0-3.5 1.5L14 9 5.8 7.2c-.5-.1-.9.1-1.1.5l-.3.5c-.2.5-.1 1 .3 1.3L9 12l-2 3H4l-1 1 3 2 2 3 1-1v-3l3-2 3.5 3.3c.3.4.8.5 1.3.3l.5-.2c.4-.3.6-.7.5-1.2z"/></svg>`,
  collection: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="8" r="6"/><path d="M15.477 12.89 17 22l-5-3-5 3 1.523-9.11"/></svg>`,
  passport: `<svg width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="2" width="16" height="20" rx="2"/><circle cx="12" cy="11" r="3"/><path d="M12 8a5 5 0 0 1 5 5"/><path d="M12 8a5 5 0 0 0-5 5"/><line x1="8" y1="17" x2="16" y2="17"/></svg>`,
};

const t = {
  en: {
    htmlTitle: 'Lumiveil — Own every mile you've ever moved',
    metaDesc: 'Every step lifts the fog. Lumiveil maps your world as you move through it — a living record of everywhere you've ever been.',
    h1a: 'Own every mile',
    h1b: "you've ever moved",
    heroDesc: 'Every step lifts the fog. Lumiveil maps your world as you move through it — a living record of everywhere you've ever been.',
    featLabel: 'Features',
    featTitle: 'Built for explorers who never stop moving',
    featSub: 'From the corner coffee run to a red-eye across continents — nothing gets left off the map.',
    features: [
      { icon: 'map',        title: 'Fog Map',           body: 'The whole world starts covered. Walk, bike, drive, ride, or fly — and watch the fog roll back in real time. Your map, earned one mile at a time.' },
      { icon: 'nfc',        title: 'NFC Stamps',        body: 'Hold your iPhone near a Lumiveil NFC tag and unlock an exclusive spot stamp. Real places, real rewards. Collecting is the new reason to explore.' },
      { icon: 'flight',     title: 'Flight Log',        body: 'Import your FlightRadar24 KML and every flight you've ever taken lands on the map. Your air routes are part of the story too.' },
      { icon: 'collection', title: 'Collections',       body: 'Airlines, airports, cities, countries, metro lines, landmarks — collect them all. Earn badges. Climb Game Center leaderboards.' },
      { icon: 'passport',   title: 'Explorer Passport', body: 'One glance tells the whole story: a timeline of every journey and a passport stamped with every country you've set foot in.' },
    ],
    stepsLabel: 'How it works',
    stepsTitle: 'Get started in three moves',
    stepsSub: 'No account. No server. Just you, your iPhone, and iCloud.',
    steps: [
      { title: 'Download and hit allow', body: 'Open Lumiveil and grant location access. That's it — your footprint log starts immediately. Everything stays on your device and in your personal iCloud. Nothing leaves.' },
      { title: 'Tap "Start Exploring" and go', body: 'Hit the button at the bottom of the map and start moving. Walk, bike, or take the train — the fog lifts automatically. Your track locks in the moment you stop.' },
      { title: 'Collect, flex, and keep going', body: 'Check your Collections for every airline, city, and landmark you've hit. Tap Stats to generate a share card and show the world where you've been.' },
    ],
    faqLabel: 'Support',
    faqTitle: 'Common questions',
    faqSub: "Still stuck? Drop us an email.",
    faqs: [
      { q: 'Does Lumiveil upload my location data?', a: 'Never. Your tracks live on your device and in your own iCloud — only you can access them. Lumiveil has no backend servers and shares nothing with anyone.' },
      { q: 'Why does Lumiveil need "Always On" location access?', a: 'So it can keep recording while your screen is off. You shouldn't have to babysit the app. You can change this in Settings anytime. When active, iOS shows a blue dot in the status bar — full transparency.' },
      { q: 'Where can I scan NFC stamps?', a: 'Lumiveil NFC tags are placed at partner venues. Tap the scan button (top-right of the map), hold your iPhone near the tag, and it unlocks instantly. We're growing the network across cities worldwide, constantly.' },
      { q: 'How do I import my flight log?', a: 'Export your FlightRadar24 history as KML (app or web), then open Lumiveil's Flight Log tab, tap "Import KML," and pick the file. Your flight history builds itself.' },
      { q: 'Can I back up or export my data?', a: 'Yes — Settings → Data Export gives you everything as GeoJSON. Take it anywhere: backup drives, other mapping apps, wherever.' },
      { q: 'Which iPhones are supported?', a: 'Lumiveil runs on iOS 18.0 or later. NFC stamp scanning works on iPhone 7 and up.' },
    ],
    contactTitle: 'Need a hand?',
    contactDesc: 'Bug, question, or a feature you can't live without — we read every message.',
    dockSections: [
      { id: 'lv-features', label: 'Features' },
      { id: 'lv-steps',    label: 'How it works' },
      { id: 'support',     label: 'Support' },
    ],
    back: '← Milifix',
    footer: '© 2026 Lumiveil. All rights reserved.',
  },
  zh: {
    htmlTitle: 'Lumiveil — 你走過的每一步，都不該被遺忘',
    metaDesc: '踏出去，霧就散了。Lumiveil 把你的移動軌跡化成地圖，每一步都算數，每一段旅程都有痕跡。',
    h1a: '你走過的每一步',
    h1b: '都不該被遺忘',
    heroDesc: '踏出去，霧就散了。Lumiveil 把你的移動軌跡化成地圖，每一步都算數，每一段旅程都有痕跡。',
    featLabel: '功能',
    featTitle: '為停不下來的探索者而生',
    featSub: '早上去巷口買咖啡，還是飛越半個地球——每一段路都留下來。',
    features: [
      { icon: 'map',        title: '霧圖',        body: '整個世界從霧中開始。走過、騎過、開車、搭車、飛越——霧就跟著退。你的地圖，一公里一公里靠自己解鎖。' },
      { icon: 'nfc',        title: 'NFC 印章',    body: '把 iPhone 靠近 Lumiveil NFC 標籤，解鎖限定景點章戳。真實地點，真實獎勵。探索從此有了新理由。' },
      { icon: 'flight',     title: '飛行記錄',    body: '匯入 FlightRadar24 KML，你飛過的每一條航線全部落地上圖。飛行軌跡也是你故事的一部分。' },
      { icon: 'collection', title: '收集系統',    body: '航空公司、機場、城市、國家、捷運線、地標——全部收進去。解鎖徽章，在 Game Center 爬排名。' },
      { icon: 'passport',   title: '探索護照',    body: '一眼看完整個故事：每段旅程的時間軸，加上你踏過的每個國家章戳。' },
    ],
    stepsLabel: '使用方式',
    stepsTitle: '三步出發',
    stepsSub: '不用帳號，不經伺服器。只有你、你的 iPhone、還有 iCloud。',
    steps: [
      { title: '下載，然後按允許', body: '打開 Lumiveil，給予位置權限，足跡記錄立刻開始。所有資料只存在你的裝置和你的 iCloud，哪裡都不傳。' },
      { title: '按下「開始探索」，走就對了', body: '點地圖底部的按鈕，開始移動。走路、騎車、搭捷運——霧自動退。停下來的瞬間，軌跡就鎖定存檔。' },
      { title: '收集、曬圖、繼續探索', body: '在收集頁看你累積的航空、城市、景點清單。到統計頁生成分享卡，告訴世界你去過哪裡。' },
    ],
    faqLabel: '支援',
    faqTitle: '常見問題',
    faqSub: '還是解決不了？直接寄信給我們。',
    faqs: [
      { q: 'Lumiveil 會上傳我的位置資料嗎？', a: '不會，永遠不會。你的軌跡只存在你的裝置和你的 iCloud，只有你能存取。Lumiveil 沒有後端伺服器，任何資料都不會傳出去。' },
      { q: '為什麼需要「一直」存取位置？', a: '這樣螢幕關掉時 App 才能繼續記錄，你不用一直盯著它。隨時可以在設定裡改。開啟後 iOS 狀態列會顯示藍點，完全透明，沒什麼好藏的。' },
      { q: 'NFC 印章在哪裡可以掃？', a: 'Lumiveil NFC 標籤放在合作景點。點地圖右上角的掃描按鈕，把 iPhone 靠近標籤，立刻解鎖。全球各城市持續擴點，持續更新。' },
      { q: '怎麼匯入飛行記錄？', a: '去 FlightRadar24（App 或網頁）把飛行歷史匯出成 KML，在 Lumiveil 的「飛行日誌」頁點「匯入 KML」，選好檔案，航班紀錄自動建立完成。' },
      { q: '資料可以備份或匯出嗎？', a: '可以。設定 → 資料匯出，把所有探索紀錄存成 GeoJSON。備份、用在別的地圖工具，隨你。' },
      { q: '支援哪些 iPhone？', a: 'Lumiveil 需要 iOS 18.0 以上。NFC 印章掃描需要 iPhone 7 以上。' },
    ],
    contactTitle: '遇到問題？',
    contactDesc: 'Bug、疑問、或一個你非要不可的新功能——每封信我們都親自看。',
    dockSections: [
      { id: 'lv-features', label: '功能' },
      { id: 'lv-steps',    label: '使用方式' },
      { id: 'support',     label: '支援' },
    ],
    back: '← Milifix',
    footer: '© 2026 Lumiveil. All rights reserved.',
  },
  ja: {
    htmlTitle: 'Lumiveil — 踏んだ道は、すべて残る',
    metaDesc: '歩けば、霧が晴れる。Lumiveil は移動のすべてを地図に刻む。どこへ行ったか、ぜんぶ。',
    h1a: '踏んだ道は、',
    h1b: 'すべて残る',
    heroDesc: '歩けば、霧が晴れる。Lumiveil は移動のすべてを地図に刻む。どこへ行ったか、ぜんぶ。',
    featLabel: '機能',
    featTitle: '動き続ける人のために作った',
    featSub: '近所のコンビニでも、深夜の国際線でも——どの移動も地図に残る。',
    features: [
      { icon: 'map',        title: 'フォグマップ',     body: '世界は霧に覆われた状態から始まる。歩いても走っても、車でも電車でも飛行機でも——動いた分だけ霧が晴れる。この地図は自分の足で稼ぐもの。' },
      { icon: 'nfc',        title: 'NFC スタンプ',    body: 'iPhone を Lumiveil の NFC タグにかざすと、限定スポットスタンプが解除される。リアルな場所、リアルなご褒美。探索に新しい理由が生まれる。' },
      { icon: 'flight',     title: 'フライトログ',     body: 'FlightRadar24 の KML をインポートすれば、これまで乗ったすべての便が地図に着陸する。空の軌跡も、あなたのストーリーの一部。' },
      { icon: 'collection', title: 'コレクション',     body: '航空会社・空港・都市・国・路線・ランドマーク——全部集める。バッジを解除して、Game Center でランキングを上り詰めろ。' },
      { icon: 'passport',   title: 'エクスプローラーパスポート', body: 'すべての旅を一覧できるタイムラインと、足を踏み入れた国のスタンプが並ぶパスポート。一目で自分の世界地図がわかる。' },
    ],
    stepsLabel: '使い方',
    stepsTitle: '3ステップで始める',
    stepsSub: 'アカウント不要。サーバーなし。あなたと iPhone と iCloud だけ。',
    steps: [
      { title: 'ダウンロードして、許可を押す', body: 'Lumiveil を開いて位置情報の許可を与えれば、すぐに記録が始まる。データはデバイスと自分の iCloud にだけ保存。どこにも送らない。' },
      { title: '「探索を開始」をタップして動く', body: 'マップ下部のボタンを押したら、あとは動くだけ。歩いても、自転車でも、電車でも——霧は自動で晴れる。止まった瞬間に軌跡が保存される。' },
      { title: 'コレクションして、シェアして、また行く', body: 'コレクションタブで航空会社・都市・ランドマークを確認。統計タブでシェアカードを作って、自分がどこに行ったか見せよう。' },
    ],
    faqLabel: 'サポート',
    faqTitle: 'よくある質問',
    faqSub: '解決しなかったらメールで。',
    faqs: [
      { q: 'Lumiveil は位置情報をアップロードしますか？', a: 'しません、絶対に。軌跡データはデバイスと自分の iCloud だけに保存される。アクセスできるのはあなただけ。Lumiveil にサーバーはなく、何も外に出ない。' },
      { q: 'なぜ「常に」位置情報が必要ですか？', a: '画面を消しても記録を続けるため。アプリをずっと開いておく必要はない。設定でいつでも変更できる。許可すると iOS のステータスバーに青いドットが出る。隠すことは何もない。' },
      { q: 'NFC スタンプはどこでスキャンできますか？', a: 'Lumiveil の NFC タグはパートナー施設に設置されている。マップ右上のスキャンボタンをタップして iPhone をタグにかざせば即解除。世界の都市で展開中、随時追加中。' },
      { q: 'フライト記録のインポート方法は？', a: 'FlightRadar24（アプリかウェブ）でフライト履歴を KML でエクスポート。Lumiveil の「フライトログ」タブで「KML をインポート」をタップしてファイルを選ぶだけ。自動で記録が完成する。' },
      { q: 'データのバックアップや書き出しはできますか？', a: 'できます。設定 → データ書き出しで、すべての探索記録を GeoJSON で書き出せる。バックアップでも、他のマップアプリでも好きに使って。' },
      { q: '対応している iPhone は？', a: 'iOS 18.0 以降が必要。NFC スタンプスキャンは iPhone 7 以降に対応。' },
    ],
    contactTitle: '困ったことがあれば',
    contactDesc: 'バグ、質問、どうしても欲しい機能——全部のメールを読んでいます。',
    dockSections: [
      { id: 'lv-features', label: '機能' },
      { id: 'lv-steps',    label: '使い方' },
      { id: 'support',     label: 'サポート' },
    ],
    back: '← Milifix',
    footer: '© 2026 Lumiveil. All rights reserved.',
  },
}[lang];

const langUrls = { en: '/lumiveil', zh: '/zh/lumiveil', ja: '/ja/lumiveil' };
---

<!DOCTYPE html>
<html lang={HTML_LANG[lang]}>
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0, viewport-fit=cover" />
  <meta name="theme-color" media="(prefers-color-scheme: dark)" content="#111318" />
  <meta name="theme-color" media="(prefers-color-scheme: light)" content="#f4f2ea" />
  <title>{t.htmlTitle}</title>
  <meta name="description" content={t.metaDesc} />
  <link rel="alternate" hreflang="en" href={langUrls.en} />
  <link rel="alternate" hreflang="zh-Hant" href={langUrls.zh} />
  <link rel="alternate" hreflang="ja" href={langUrls.ja} />
  <script is:inline>
    (function () {
      try {
        var t = localStorage.getItem('portfolio-theme');
        document.documentElement.dataset.theme = t === 'light' ? 'light' : t === 'dark' ? 'dark' : (window.matchMedia('(prefers-color-scheme: light)').matches ? 'light' : 'dark');
      } catch (e) {
        document.documentElement.dataset.theme = 'dark';
      }
    })();
  </script>
  <style>
    *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }

    :root {
      --sans: 'Space Grotesk', sans-serif;
      --mono: 'Space Mono', monospace;
      --lv-accent: #39C5BB;
      --lv-accent-dim: rgba(57, 197, 187, 0.12);
      --lv-accent-border: rgba(57, 197, 187, 0.28);
      --lv-card: color-mix(in srgb, var(--bg2) 80%, var(--bg3));
    }

    html { scroll-behavior: smooth; }

    body {
      font-family: var(--sans);
      background: var(--bg);
      color: var(--text);
      -webkit-font-smoothing: antialiased;
      line-height: 1.7;
      cursor: none;
    }

    #cursor {
      width: 8px; height: 8px; background: var(--lv-accent);
      border-radius: 50%; position: fixed; pointer-events: none;
      z-index: 9999; transform: translate(-50%, -50%);
      transition: width 0.2s, height 0.2s;
    }
    #cursor-ring {
      width: 32px; height: 32px; border: 1px solid rgba(57,197,187,0.4);
      border-radius: 50%; position: fixed; pointer-events: none;
      z-index: 9998; transform: translate(-50%, -50%);
      transition: transform 0.12s ease-out, width 0.25s, height 0.25s;
    }
    body:has(a:hover) #cursor { width: 12px; height: 12px; }
    body:has(a:hover) #cursor-ring { width: 48px; height: 48px; }

    @media (max-width: 768px) {
      body { cursor: auto !important; }
      #cursor, #cursor-ring { display: none !important; }
    }

    .divider { border: none; border-top: 1px solid var(--line); margin: 0 24px; }

    /* ── Hero ── */
    .lv-hero {
      position: relative;
      text-align: center;
      padding: 130px 24px 110px;
      overflow: hidden;
    }

    /* Animated radial glow behind hero */
    .lv-hero::before {
      content: '';
      position: absolute;
      inset: 0;
      background: radial-gradient(ellipse 75% 60% at 50% 0%, rgba(57,197,187,0.14) 0%, transparent 70%);
      animation: lv-glow-pulse 5s ease-in-out infinite alternate;
      pointer-events: none;
    }

    @keyframes lv-glow-pulse {
      from { opacity: 0.7; transform: scaleX(1) scaleY(1); }
      to   { opacity: 1;   transform: scaleX(1.08) scaleY(1.12); }
    }

    /* Floating orbs */
    .lv-hero__orb {
      position: absolute;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(57,197,187,0.18) 0%, transparent 70%);
      pointer-events: none;
    }
    .lv-hero__orb--a {
      width: 320px; height: 320px;
      top: -80px; left: calc(50% - 400px);
      animation: lv-orb-a 8s ease-in-out infinite alternate;
    }
    .lv-hero__orb--b {
      width: 240px; height: 240px;
      top: 20px; right: calc(50% - 380px);
      animation: lv-orb-b 10s ease-in-out infinite alternate;
    }

    @keyframes lv-orb-a {
      from { transform: translate(0, 0) scale(1); }
      to   { transform: translate(20px, 30px) scale(1.1); }
    }
    @keyframes lv-orb-b {
      from { transform: translate(0, 0) scale(1); }
      to   { transform: translate(-15px, 20px) scale(0.92); }
    }

    /* Animated grid lines overlay */
    .lv-hero::after {
      content: '';
      position: absolute;
      inset: 0;
      background-image:
        linear-gradient(rgba(57,197,187,0.04) 1px, transparent 1px),
        linear-gradient(90deg, rgba(57,197,187,0.04) 1px, transparent 1px);
      background-size: 60px 60px;
      mask-image: radial-gradient(ellipse 80% 80% at 50% 0%, black 0%, transparent 75%);
      pointer-events: none;
      animation: lv-grid-drift 12s ease-in-out infinite alternate;
    }

    @keyframes lv-grid-drift {
      from { background-position: 0 0; }
      to   { background-position: 15px 15px; }
    }

    .lv-hero__inner {
      position: relative;
      z-index: 1;
    }

    .lv-hero h1 {
      font-size: clamp(2.6rem, 7vw, 4.8rem);
      font-weight: 800; letter-spacing: 0.02em; line-height: 1.28;
      margin-bottom: 28px;
      animation: lv-hero-in 0.9s cubic-bezier(0.22, 1, 0.36, 1) both;
    }

    @keyframes lv-hero-in {
      from { opacity: 0; transform: translateY(24px); }
      to   { opacity: 1; transform: translateY(0); }
    }

    .lv-hero h1 em {
      font-style: normal;
      color: var(--lv-accent);
      position: relative;
    }

    /* Underline accent on em */
    .lv-hero h1 em::after {
      content: '';
      position: absolute;
      bottom: -4px; left: 0; right: 0;
      height: 3px;
      background: linear-gradient(90deg, var(--lv-accent), rgba(57,197,187,0));
      border-radius: 2px;
      animation: lv-underline-in 1.2s 0.4s cubic-bezier(0.22, 1, 0.36, 1) both;
    }

    @keyframes lv-underline-in {
      from { transform: scaleX(0); transform-origin: left; }
      to   { transform: scaleX(1); transform-origin: left; }
    }

    .lv-hero__desc {
      max-width: 520px; margin: 0 auto;
      font-size: 1.1rem; color: var(--muted); line-height: 1.75;
      animation: lv-hero-in 0.9s 0.15s cubic-bezier(0.22, 1, 0.36, 1) both;
    }

    /* ── Section ── */
    .lv-section { padding: 96px 24px; max-width: 1100px; margin: 0 auto; }
    .lv-section-label {
      font-family: var(--mono); font-size: 0.72rem; font-weight: 700;
      letter-spacing: 0.18em; text-transform: uppercase;
      color: var(--lv-accent); margin-bottom: 12px;
    }
    .lv-section-title {
      font-size: clamp(1.7rem, 4vw, 2.6rem);
      font-weight: 800; letter-spacing: -0.02em; line-height: 1.18; margin-bottom: 16px;
    }
    .lv-section-sub { color: var(--muted); max-width: 500px; margin-bottom: 56px; font-size: 1rem; line-height: 1.7; }

    /* ── Feature rows ── */
    .lv-feature-grid { display: flex; flex-direction: column; gap: 2px; }

    .lv-card {
      display: grid;
      grid-template-columns: 64px 220px 1fr;
      align-items: center;
      gap: 0;
      border: 1px solid var(--line2);
      border-radius: 16px;
      overflow: hidden;
      transition: border-color 0.22s, background 0.22s;
      background: var(--lv-card);
    }

    .lv-card:first-child { border-radius: 16px 16px 6px 6px; }
    .lv-card:last-child  { border-radius: 6px 6px 16px 16px; }
    .lv-card:not(:first-child):not(:last-child) { border-radius: 6px; }

    .lv-card:hover {
      border-color: var(--lv-accent-border);
      background: color-mix(in srgb, var(--lv-card) 85%, rgba(57,197,187,0.06));
    }

    .lv-card__icon {
      width: 64px; height: 64px;
      display: flex; align-items: center; justify-content: center;
      color: var(--lv-accent);
      flex-shrink: 0;
      border-right: 1px solid var(--line2);
    }

    .lv-card__title {
      padding: 20px 24px;
      font-size: 0.95rem; font-weight: 700; letter-spacing: -0.01em;
      border-right: 1px solid var(--line2);
      height: 100%;
      display: flex; align-items: center;
    }

    .lv-card__body {
      padding: 16px 28px;
      color: var(--muted); font-size: 0.9rem; line-height: 1.65;
    }

    @media (max-width: 680px) {
      .lv-card {
        grid-template-columns: 52px 1fr;
        grid-template-rows: auto auto;
      }
      .lv-card__icon { width: 52px; height: 52px; border-right: none; border-bottom: 1px solid var(--line2); }
      .lv-card__title { padding: 14px 16px; border-right: none; border-bottom: none; height: auto; }
      .lv-card__body { grid-column: 1 / -1; padding: 0 16px 16px; border-top: 1px solid var(--line2); }
    }

    /* ── Steps ── */
    .lv-steps { display: flex; flex-direction: column; gap: 0; }
    .lv-step { display: grid; grid-template-columns: 52px 1fr; gap: 20px; position: relative; padding-bottom: 44px; }
    .lv-step:last-child { padding-bottom: 0; }
    .lv-step__num {
      width: 52px; height: 52px; border-radius: 50%;
      background: var(--lv-accent-dim); border: 2px solid var(--lv-accent);
      display: flex; align-items: center; justify-content: center;
      font-family: var(--mono); font-size: 0.95rem; font-weight: 700; color: var(--lv-accent);
      flex-shrink: 0;
    }
    .lv-step__line {
      position: absolute; left: 26px; top: 52px; bottom: 0;
      width: 2px; background: var(--lv-accent-border);
    }
    .lv-step:last-child .lv-step__line { display: none; }
    .lv-step__content { padding-top: 10px; }
    .lv-step__content h3 { font-size: 1.05rem; font-weight: 700; margin-bottom: 6px; letter-spacing: -0.01em; }
    .lv-step__content p { color: var(--muted); font-size: 0.92rem; line-height: 1.68; }

    /* ── FAQ ── */
    .lv-faq { display: flex; flex-direction: column; gap: 12px; }
    details {
      background: var(--lv-card); border: 1px solid var(--line2);
      border-radius: 16px; overflow: hidden; transition: border-color 0.2s;
    }
    details[open] { border-color: var(--lv-accent-border); }
    summary {
      padding: 20px 24px; font-weight: 600; font-size: 0.95rem;
      cursor: pointer; list-style: none;
      display: flex; justify-content: space-between; align-items: center; gap: 16px;
    }
    summary::-webkit-details-marker { display: none; }
    summary::after {
      content: "+"; color: var(--lv-accent);
      font-size: 1.3rem; font-weight: 300; flex-shrink: 0; line-height: 1;
      font-family: var(--mono);
    }
    details[open] summary::after { content: "−"; }
    .lv-faq-body { padding: 0 24px 20px; color: var(--muted); font-size: 0.92rem; line-height: 1.72; }

    /* ── Contact (smaller) ── */
    .lv-contact {
      background: var(--lv-card); border: 1px solid var(--lv-accent-border);
      border-radius: 20px; padding: 36px 40px; text-align: center;
    }
    .lv-contact h2 { font-size: 1.3rem; font-weight: 800; letter-spacing: -0.01em; margin-bottom: 8px; }
    .lv-contact p { color: var(--muted); margin-bottom: 24px; font-size: 0.95rem; }
    .lv-contact-email {
      display: inline-block;
      background: var(--lv-accent-dim); border: 1px solid var(--lv-accent-border);
      color: var(--lv-accent); font-family: var(--mono);
      font-size: 0.82rem; font-weight: 700; letter-spacing: 0.04em;
      padding: 10px 24px; border-radius: 100px; text-decoration: none;
      transition: background 0.2s;
    }
    .lv-contact-email:hover { background: rgba(57,197,187,0.2); }

    /* ── Footer ── */
    .lv-footer {
      text-align: center; padding: 36px 24px;
      color: var(--muted-dim); font-family: var(--mono);
      font-size: 0.75rem; letter-spacing: 0.06em;
      border-top: 1px solid var(--line);
    }

    @media (max-width: 640px) {
      .lv-hero { padding: 88px 24px 72px; }
      .lv-section { padding: 72px 24px; }
      .lv-contact { padding: 28px 20px; }
      .lv-hero__orb { display: none; }
    }

    @media (prefers-reduced-motion: reduce) {
      .lv-hero::before, .lv-hero::after,
      .lv-hero__orb, .lv-hero h1, .lv-hero__desc,
      .lv-hero h1 em::after { animation: none !important; }
    }
  </style>
</head>
<body class="portfolio-atmo">
  <AmbientBackdrop />
  <div id="cursor"></div>
  <div id="cursor-ring"></div>

  <SiteNav variant="platform" lang={lang} pathname={pathname} />

  <PortfolioChrome
    lang={lang}
    pathname={pathname}
    dockSections={t.dockSections}
    dockHomeHref={lang === 'en' ? '/' : `/${lang}/`}
    dockHomeLabel="Milifix"
    dockHomeIcon="home"
    dockOrder="sections-first"
  />

  <!-- Hero -->
  <div class="lv-hero">
    <div class="lv-hero__orb lv-hero__orb--a"></div>
    <div class="lv-hero__orb lv-hero__orb--b"></div>
    <div class="lv-hero__inner">
      <h1><em>{t.h1a}</em><br>{t.h1b}</h1>
      <p class="lv-hero__desc">{t.heroDesc}</p>
    </div>
  </div>

  <hr class="divider" />

  <!-- Features -->
  <section class="lv-section" id="lv-features">
    <p class="lv-section-label">{t.featLabel}</p>
    <h2 class="lv-section-title">{t.featTitle}</h2>
    <p class="lv-section-sub">{t.featSub}</p>
    <div class="lv-feature-grid">
      {t.features.map((f) => (
        <div class="lv-card">
          <div class="lv-card__icon" set:html={ICONS[f.icon as keyof typeof ICONS]} />
          <div class="lv-card__title">{f.title}</div>
          <div class="lv-card__body">{f.body}</div>
        </div>
      ))}
    </div>
  </section>

  <hr class="divider" />

  <!-- How it works -->
  <section class="lv-section" id="lv-steps">
    <p class="lv-section-label">{t.stepsLabel}</p>
    <h2 class="lv-section-title">{t.stepsTitle}</h2>
    <p class="lv-section-sub">{t.stepsSub}</p>
    <div class="lv-steps">
      {t.steps.map((s, i) => (
        <div class="lv-step">
          <div class="lv-step__num">{String(i + 1).padStart(2, '0')}</div>
          <div class="lv-step__line"></div>
          <div class="lv-step__content">
            <h3>{s.title}</h3>
            <p>{s.body}</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <hr class="divider" />

  <!-- FAQ -->
  <section class="lv-section" id="support">
    <p class="lv-section-label">{t.faqLabel}</p>
    <h2 class="lv-section-title">{t.faqTitle}</h2>
    <p class="lv-section-sub">{t.faqSub}</p>
    <div class="lv-faq">
      {t.faqs.map((f) => (
        <details>
          <summary>{f.q}</summary>
          <div class="lv-faq-body">{f.a}</div>
        </details>
      ))}
    </div>
  </section>

  <hr class="divider" />

  <!-- Contact -->
  <section class="lv-section">
    <div class="lv-contact">
      <h2>{t.contactTitle}</h2>
      <p>{t.contactDesc}</p>
      <a class="lv-contact-email" href="mailto:steven33152239@gmail.com">
        steven33152239@gmail.com
      </a>
    </div>
  </section>

  <footer class="lv-footer">{t.footer}</footer>

  <script>
    import { initPortfolioCursor } from '../../scripts/portfolio-cursor';
    import { initTheme } from '../../scripts/theme-init';
    initTheme();
    initPortfolioCursor();
  </script>
</body>
</html>

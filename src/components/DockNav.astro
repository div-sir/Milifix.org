---
import '../styles/dock-nav.css';
import type { Lang } from '../i18n/types';
import { ui } from '../i18n/ui';
import { homePath, parseLocalizedPath } from '../i18n/routes';

export interface DockSection {
  id: string;
  label: string;
  href?: string;
  icon?: 'back';
}

interface Props {
  lang: Lang;
  pathname: string;
  dockSections: DockSection[];
  dockSectionLinkMode?: 'hash' | 'scroll';
  /** 首顆連結網址（預設平台首頁） */
  dockHomeHref?: string;
  /** 首顆連結文案（預設 dockHome） */
  dockHomeLabel?: string;
  dockHomeIcon?: 'home' | 'back';
  dockOrder?: 'home-first' | 'sections-first';
  /** 首屏封面／hero 頁面可關閉，與版面內捲動提示分工 */
  showScrollTop?: boolean;
  /** 分頁式頁面（如旅遊首頁的 tab）自行管理區塊切換，需停用捲動 spy 以免它移除 hash */
  disableSectionSpy?: boolean;
}

const {
  lang,
  pathname,
  dockSections,
  dockSectionLinkMode = 'hash',
  dockHomeHref: dockHomeHrefProp,
  dockHomeLabel: dockHomeLabelProp,
  dockHomeIcon = 'home',
  dockOrder = 'home-first',
  showScrollTop = true,
  disableSectionSpy = false,
} = Astro.props;
const useScrollSections = dockSectionLinkMode === 'scroll';
const N = ui(lang).nav;
const { logicalPath } = parseLocalizedPath(pathname);
const lp = logicalPath.replace(/\/$/, '') || '/';
const homeHref = dockHomeHrefProp ?? homePath(lang);
const homeLabel = dockHomeLabelProp ?? N.dockHome;
const { logicalPath: dockTargetLogical } = parseLocalizedPath(homeHref);
const dockTargetKey = dockTargetLogical.replace(/\/$/, '') || '/';
const onDockHome = lp === dockTargetKey;
const hasSections = dockSections.length > 0;
---

<nav class="dock-nav dock-pill-nav" aria-label={N.dockAria} data-no-section-spy={disableSectionSpy ? '' : undefined}>
  <div class="dock-pill-nav__shell">
    <ul class="dock-pill-nav__row" role="menubar">
      {
        dockOrder === 'sections-first' &&
          hasSections &&
          dockSections.map((s) => (
            <li class="dock-pill-nav__cell" role="none">
              {s.href ? (
                <a
                  class:list={[
                    'dock-pill-nav__pill',
                    'dock-nav__section',
                    s.icon ? 'dock-pill-nav__pill--icon' : undefined,
                  ]}
                  href={s.href}
                >
                  {s.icon === 'back' ? (
                    <span class="dock-pill-nav__home-icon" aria-hidden="true">
                      <svg width="17" height="17" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                        <path
                          d="M14.5 4.5L7 12l7.5 7.5M7 12h12"
                          stroke="currentColor"
                          stroke-width="1.65"
                          stroke-linecap="round"
                          stroke-linejoin="round"
                        />
                      </svg>
                    </span>
                  ) : (
                    <span class="dock-pill-nav__label-stack">
                      <span class="dock-pill-nav__label">{s.label}</span>
                      <span class="dock-pill-nav__label dock-pill-nav__label--hover" aria-hidden="true">
                        {s.label}
                      </span>
                    </span>
                  )}
                  <span class="dock-pill-nav__active-dot" aria-hidden="true" />
                </a>
              ) : useScrollSections ? (
                <button
                  type="button"
                  class="dock-pill-nav__pill dock-nav__section"
                  data-dock-scroll-id={s.id}
                >
                  <span class="dock-pill-nav__label-stack">
                    <span class="dock-pill-nav__label">{s.label}</span>
                    <span class="dock-pill-nav__label dock-pill-nav__label--hover" aria-hidden="true">
                      {s.label}
                    </span>
                  </span>
                  <span class="dock-pill-nav__active-dot" aria-hidden="true" />
                </button>
              ) : (
                <a class="dock-pill-nav__pill dock-nav__section" href={`#${s.id}`}>
                  <span class="dock-pill-nav__label-stack">
                    <span class="dock-pill-nav__label">{s.label}</span>
                    <span class="dock-pill-nav__label dock-pill-nav__label--hover" aria-hidden="true">
                      {s.label}
                    </span>
                  </span>
                  <span class="dock-pill-nav__active-dot" aria-hidden="true" />
                </a>
              )}
            </li>
          ))
      }

      {
        dockOrder === 'sections-first' && hasSections && (
          <li class="dock-pill-nav__sep-cell" aria-hidden="true">
            <span class="dock-pill-nav__vsep" />
          </li>
        )
      }

      <li class="dock-pill-nav__cell" role="none">
        <a
          class:list={['dock-pill-nav__pill', 'dock-pill-nav__pill--home']}
          href={homeHref}
          aria-current={onDockHome ? 'page' : undefined}
          aria-label={homeLabel}
        >
          <span class="dock-pill-nav__home-icon" aria-hidden="true">
            {
              dockHomeIcon === 'back' ? (
                <svg width="17" height="17" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path
                    d="M15.5 5.5L8.5 12l7 6.5M9.5 12H21"
                    stroke="currentColor"
                    stroke-width="1.65"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                </svg>
              ) : (
                <svg width="17" height="17" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path
                    d="M3 10.5L12 3l9 7.5V20a1 1 0 0 1-1 1h-5v-6H8v6H4a1 1 0 0 1-1-1v-9.5z"
                    stroke="currentColor"
                    stroke-width="1.65"
                    stroke-linejoin="round"
                  />
                </svg>
              )
            }
          </span>
        </a>
      </li>

      {
        dockOrder !== 'sections-first' &&
          hasSections &&
          dockSections.map((s) => (
            <li class="dock-pill-nav__cell" role="none">
              {s.href ? (
                <a
                  class:list={[
                    'dock-pill-nav__pill',
                    'dock-nav__section',
                    s.icon ? 'dock-pill-nav__pill--icon' : undefined,
                  ]}
                  href={s.href}
                >
                  {s.icon === 'back' ? (
                    <span class="dock-pill-nav__home-icon" aria-hidden="true">
                      <svg width="17" height="17" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                        <path
                          d="M14.5 4.5L7 12l7.5 7.5M7 12h12"
                          stroke="currentColor"
                          stroke-width="1.65"
                          stroke-linecap="round"
                          stroke-linejoin="round"
                        />
                      </svg>
                    </span>
                  ) : (
                    <span class="dock-pill-nav__label-stack">
                      <span class="dock-pill-nav__label">{s.label}</span>
                      <span class="dock-pill-nav__label dock-pill-nav__label--hover" aria-hidden="true">
                        {s.label}
                      </span>
                    </span>
                  )}
                  <span class="dock-pill-nav__active-dot" aria-hidden="true" />
                </a>
              ) : useScrollSections ? (
                <button
                  type="button"
                  class="dock-pill-nav__pill dock-nav__section"
                  data-dock-scroll-id={s.id}
                >
                  <span class="dock-pill-nav__label-stack">
                    <span class="dock-pill-nav__label">{s.label}</span>
                    <span class="dock-pill-nav__label dock-pill-nav__label--hover" aria-hidden="true">
                      {s.label}
                    </span>
                  </span>
                  <span class="dock-pill-nav__active-dot" aria-hidden="true" />
                </button>
              ) : (
                <a class="dock-pill-nav__pill dock-nav__section" href={`#${s.id}`}>
                  <span class="dock-pill-nav__label-stack">
                    <span class="dock-pill-nav__label">{s.label}</span>
                    <span class="dock-pill-nav__label dock-pill-nav__label--hover" aria-hidden="true">
                      {s.label}
                    </span>
                  </span>
                  <span class="dock-pill-nav__active-dot" aria-hidden="true" />
                </a>
              )}
            </li>
          ))
      }

      {
        hasSections && showScrollTop && (
          <li class="dock-pill-nav__sep-cell" aria-hidden="true">
            <span class="dock-pill-nav__vsep" />
          </li>
        )
      }

      {
        showScrollTop && (
          <li class="dock-pill-nav__cell" role="none">
            <button
              type="button"
              class="dock-pill-nav__pill dock-pill-nav__pill--top"
              id="dock-nav-scroll-top"
              aria-label={N.dockScrollTop}
              title={N.dockScrollTop}
            >
              <span class="dock-pill-nav__home-icon" aria-hidden="true">
                <svg width="17" height="17" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path
                    d="M7 14.5L12 9l5 5.5"
                    stroke="currentColor"
                    stroke-width="1.65"
                    stroke-linecap="round"
                    stroke-linejoin="round"
                  />
                </svg>
              </span>
            </button>
          </li>
        )
      }

    </ul>
  </div>
</nav>

<script>
  import { initDockSectionSpy } from '../scripts/dock-section-spy';
  import { closeMobileSiteNavFlyout } from '../scripts/site-nav-client';

  const nav = document.querySelector<HTMLElement>('.dock-nav');
  // 分頁式頁面自行管理切換（並設定 hash），停用 spy 以免它捲動 + 移除 hash 造成衝突
  if (nav && nav.dataset.noSectionSpy === undefined) initDockSectionSpy(nav);

  document.getElementById('dock-nav-scroll-top')?.addEventListener('click', () => {
    closeMobileSiteNavFlyout();
    const instant = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
    window.scrollTo({ top: 0, behavior: instant ? 'auto' : 'smooth' });
    document.getElementById('main-content')?.focus({ preventScroll: true });
  });
</script>
